# CLAUDE.md — Obsidian Vault 长期整理规范

本文件为 AI（Claudian）在本 Vault 工作的长期记忆。用户用中文交流，笔记常含大量「自我讲解 + 例子」，整理时**保留通俗解释并补速查表/表格**。

## 澳门大学课程目录三分结构

`澳门大学课程/` 下固定分三类子目录：

| 目录 | 内容 | 规则 |
|---|---|---|
| `原始随记/` | 课堂随手记原文，每门课一个文件 | 用户自己写；零散、含口语解释；**整理后保留不删**；Claudian 可在顶部维护 Hub 索引与统一页眉 |
| `整理笔记/` | Claudian 按主题重组的结构化笔记 | 每篇自包含 + 课堂例子 + 速查表；文件名带 `课程代码-序号-主题` |
| `课程规划/` | 选课/培养方案/分享会等规划材料 | 不做主题化整理；语音转写原文保持原样 |

## 命名与拼写

- **课程代码一律 `CISC`（不是 `CSIC`）**，文件名、标题、互链都遵循。
- 随记：`课程代码-课程名.md`（如 `CISC7021-Applied Natural Language Processing.md`）。
- 整理笔记：`课程代码-主题.md`；同课系列加两位序号（`CISC7021-NLP-01 …`）。
- 跨课产物以产出课程为前缀（如 `AHGC7315-NLP 专业词汇笔记（计算语言学）.md`）。

## 「整理」的标准工作流

1. 读 `原始随记/对应课程.md`；
2. 按主题拆成多篇，写入 `整理笔记/`（自包含 + 例子 + 速查表）；
3. 随记原文保留，不删改；
4. 在随记顶部更新 Hub「已整理主题笔记」表格；
5. 每次整理后顺手更新 `澳门大学课程/_整理规范.md` 里的「全课索引表」。

## 附件

图片一律 basename 嵌入 `![[Pasted image xxx.png]]`；Obsidian 全库按文件名解析，移动目录不影响显示。课堂共享图片在 `原始随记/附件/`。

## 详细文档

人读版规范见 `澳门大学课程/_整理规范.md`（含目标目录树、命名规则、一次整理步骤、全课索引）。
