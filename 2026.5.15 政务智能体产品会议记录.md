# 2026.5.15 政务智能体 · 产品经理会议记录

> **原始来源**：`2026.5.15 政务智能体产品经理知道.m4a` 录音转文字  
> **整理日期**：2026-06-24  
> **说明**：本文由会议录音自动转写后，仅做分段排版与结构梳理，未对原始文字内容做实质性修改。

---

## 一、技术趋势：模型变小、算力门槛骤降

其实从过去一年多的观察来看，技术的突破和快速扩散、还有迭代速度非常快。你看去年 2 月份的时候，第一次火的是大模型的基础能力，突然就出现了。然后今年 2 月份突然爆火的是 Agent，它可以调动工具，能够做更多的事。

我们现在干的活，实际上是去年基于基础模型能做的事情，今年我们就可以跟上用 Agent 这个工具做各种事了。

肉眼可见就是这种"一年一突破"，但后面可能用不到一年，半年左右新的技术可能就出现了。技术快速迭代的这个过程本身就是我们必须考虑的第一个变量——不可能说做一个东西扔出去，别人说"哎呀现在有一个新的东西，我不用你这个东西了"。

### 现在的最新趋势

我跟你讲一下最新的研究进展，我一直跟进这些年，也跟各个大公司在聊：

1. **硬件依赖直接下降**。以前做基础模型需要 H100 或者 910B 这种几百万的显卡才能跑完整模型。现在不需要了，只要有一张小卡——比如 3090 24G——就能调出效果非常好的大模型，参数可能就 20 亿 30 亿，但在这个领域上效果不输百亿模型。

2. **参数规模不构成智能壁垒**。这两天大家开始用几 B 甚至零点几 B 的模型。参数本身不构成智能，关键在于你给的指令规则足够强，你提供的训练数据足够精细且有针对性。

3. **参数小了就不需要特别强的硬件**。一张 3090 24G 既能部署各种调好的模型，同时还可以自己做训练。现在真正的关键变成了数据——你输入的那些训练问答数据，反映在特定场景下面的效果。

4. **我们现有的硬件完全可以胜任**。24G 显存算"高配"了，4G、6G、8G 显存都能跑。我们在政务网上本地部署，用国产硬件完全没有问题。

> **小结**：技术趋势是——算力门槛持续降低，数据质量和场景知识成为真正的壁垒。

---

## 二、AI 研究的最新方向：从"训练为王"到"推理为王"

### 过去 vs 现在

以前的做法：需要海量数据做大规模型训练，十亿级的参数需要十亿级甚至更多的有效数据，数据量是有天花板的。

**今年的新方向（2026 年 4-5 月世界顶级研究共识）**：不再依赖于大规模训练，而是靠推理阶段——你给定的指令质量、你定义的方式，决定智能水平和执行任务的效率能力。

### 英伟达为什么急着卖中国？

昨天黄仁勋在最后一天最后一刻搭飞机跑过来，最重要的一件事就是卖我们卡。为什么美国会放行、为什么他一定要卖中国？

如果这个方向（推理优先）成为共识的话，这种用于训练的超大规模高性能显卡就不需要了。你训练一次模型出来之后，不需要反复训练，那些集群可以拿出来专门做推理。

**那么真正有价值的是什么？** 是每一个日常应用场景、推理场景下的数据和知识——人是怎么想这个问题的、到这个场景应该做什么、知道什么。这些知识的价值就超过了算力和算法的价值。

---

## 三、产品核心理念：数字员工 = 低成本替代"有一点判断"的工作

回到我们怎么做产品这件事，核心是两个问题：

### 问题一：哪些场景能积累数据和经验？

数据本身是有价值、有治理意义的。这里面分两种情况：

- **常规性的东西**：写完就 OK
- **非常规的东西**：会有意外情况，需要做判断

这两种知识都是有价值的，而且这是我们的核心壁垒。

### 问题二：什么情况下用数字员工替代人？

替代人本质上就一件事：**这东西不是完全标准化、需要加入一点判断和修改的**。只要有一点判断，就可以用大模型替代，而且是低成本替代。

以前大模型需要大量算力，成本很高。现在成本降下来了，我可以为某个岗位定制模型。

**算一笔账**：一个岗位月薪 1 万，假设有 22 个人。我可以用大模型替代，每个月为它花 100 块、1000 块（其实就是电费），成本是人工的 1/10 甚至 1%。这不是由模型能力决定的，而是由"同等能力或接近能力"的成本决定的——这就是工具化的成本优势。

### 优先替代什么？

1. 优先替代那些**数据和知识最有价值**的岗位
2. 在正式运行岗位里，找到**行为不完全程序化、需要一点判断**的环节
3. 把判断用知识的方式固化进模型

我们**不调整程序逻辑**，我们就是在某个位置上把这个环节替代掉——你要达到的是能力，怎么替代是业务逻辑的事。

### 为什么可以这样做？

很简单，因为所有操作本质上都是文本。人无论怎么操作，说到底就是一套文本。模型处理的就是这套文本。

---

## 四、产品设计：一个自迭代的数据闭环体系

### 数字员工助手的运作方式

所谓的"助手"是这样用的：使用者必须基于我们提供的工具来操作，我们通过这个系统间接映射到目标系统。这样：

- 所有前端输入的数据都能存下来
- 积累几个月后，就可以完全替代人的操作
- 做到"知识痕迹"和"操作痕迹"的双重积累

### 核心逻辑：数据飞轮

整个系统是一个"输入 → 生成 → 修改 → 回传"的循环：

```
使用者输入数据 → 系统自动生成内容 → 使用者修改 → 
修改结果回传后台 → 后台积累问答对 → 优化提示词/再训练 → 
系统变得更智能 → 使用者越来越少修改 → 最终一键完成
```

每隔两三个月，基于积累的数据手工做一次优化——优化提示词、优化知识库、优化记忆/技能，然后系统就更智能了。这就是一个"手工辅助 + 数据积累"的自净化循环。

### 三个核心功能

我理解你的初代产品，本质上就三个功能：

#### 功能一：一键生成填充（插件形态）

面向特定填表场景的插件。使用者不知道要填什么的时候，点一下 AI 助手，自动生成内容填入各输入框。

- **像角色扮演 AI 那样简单**：现在很多 AI 对话应用里，你不知道怎么回的时候点一下，自动生成一句符合语境的话。填表是一模一样的逻辑。
- **要求**：在流程中强制使用者必须点一下生成 → 检查 → 修改 → 确认，不能跳过。
- **关键约束**：所有生成的、修改过的内容必须保存下来。

#### 功能二：数据回传与保存

所有发出的内容，必须能够取回来。**这是真正的价值所在。**

原因：对于模型微调来说，需要至少 1 万条交互数据才能把一个方向优化到极致（1 万条是底线）。你没办法用正常方式拿到这些数据，别人也不会为你写。所以只能嵌入到他们现有的业务工具里去解决这个问题。

#### 功能三：配置界面（Web 端）

不可能让使用者直接接触后台或黑箱。需要一个 Web 管理界面，用来：

- 配置 API 连接大模型
- 注入提示词
- 设定每个输入框的映射关系
- 定义输入是什么、输出格式要求是什么

> **简单原则**：不要把任何复杂操作放在前端。所有在后台配置好，前端只管"一键填充"。速度要快，够用就行——能达到 60 分就好，能替代部分人工，人稍微改改就可以。

---

## 五、产品策略：从"够用"起步，数据驱动进化

### 为什么是 60 分而不是满分？

- 前端越简单、速度越快，使用者越愿意用
- 能达到 60 分（替代部分人工）就已经有价值了
- 人的修改本身是确定性的——你改了什么，后台就能学到

### 数据闭环的价值链

```
输入 → 输出 → 修改后的输出（三者构成一条"问答对"）
                                    ↓
                          持续积累到本地
                                    ↓
                    为这个业务场景做优化
                    ① 优化提示词（基于真实数据调优规则）
                    ② 可能增加工具调用（如果发现现有工具不够）
                                    ↓
                    数据量够了（~1万条）→ 微调模型
                                    ↓
                    从 7B 模型 → 3B → 1.5B → 0.5B 越做越小
                                    ↓
              多个功能组合 → 智能体集群 → 能力升级
```

### 终极形态

这套自迭代体系运转起来后：

- 中心智能体越来越强，赋能到每一个端的能力越来越强
- 不管要不要裁人，大模型构成的这套体系可以全线替代
- 哪怕大的体制结构变了，这个体系也能继续运转

**这就是不可替代的核心壁垒**——海量的场景数据 + 持续进化的垂域模型。

---

## 六、插件的实现方式

参考现有做法（如 cloud 的侧边栏模式）：

- 在网页侧边安装插件栏
- 使用者触发插件 → 一键运行 → 所有输入框自动填充
- **不需要视觉识别**：不用像 cloud 那样截屏识别（太复杂），而是提前在后台把输入框的映射关系配置好
- **唯一调用的是大模型** + 提示词 + 映射填充逻辑

### 多场景复用

- 表单填充场景 → 一键填入
- 客服问答场景 → 一键生成回复
- 问题 + AI 答案 + 修改后的答案 → 三条数据回传

不同场景只有表面的差异，本质一样：输入 → AI 生成 → 人修改 → 输出结果 + 修改记录回传。

---

## 七、关于现有流程的讨论

> （此处涉及具体业务系统流程的讨论，包含图像识别、报送等环节）

### 核心观察

1. **泛化的关键是解耦**：如果流程很重，泛化的成本和复杂度就很高。解耦之后才有"专用能力"和"通用能力"的分离——越轻泛化能力越强。如果想把方法泛化成插件，必须找出体系里真正核心的问题。

2. **真正核心的是两个东西**：
   - **识别能力**（什么时候应该去看摄像头、去识别）
   - **报送能力**（通用能力，不依赖具体场景）

3. **唯一需要"智能决策"的环节**是：得到信息（如气象预警）→ 判断要不要做某事 → 这个预判环节适合自动化替代。

4. **多模态识别的准确率**：目前最好的是 90% 左右，有 10% 的错漏。所以剩下需要人工标注。

### 关于智能员工

- 能力完全在前端就能具备，前端需求非常大
- 不需要装复杂系统，只需大模型 + 提示词
- 前端积累的判断行为和提示词都可以积累在后台
- 维护时能做到一键更新

---

## 八、总结：当前产品要做的

| 维度 | 要点 |
|------|------|
| **产品形态** | 轻量插件 + Web 配置界面 + 后台数据管理 |
| **核心功能** | 一键 AI 填充、数据回传、配置管理 |
| **目标用户** | 政务业务部门的日常操作人员 |
| **初始定位** | 辅助工具（减负），不是替代 |
| **数据策略** | 嵌入流程、持续积累问答对、达到 1 万条后微调模型 |
| **进化路径** | 60 分起步 → 数据积累 → 提示词优化 → 模型微调 → 垂域专用模型 |
| **核心壁垒** | 场景数据 + 持续进化的自迭代体系 |
| **商业模式** | 先用免费/低价工具占据场景 → 数据积累 → 训练专属模型 → 卖高价值产品 |

---

*原始录音：2026.5.15 政务智能体产品经理知道.m4a*  
*转录 + 排版：2026-06-24*
